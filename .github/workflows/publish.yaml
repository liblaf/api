# This file is @generated by <https://github.com/liblaf/copier-typescript>.
# DO NOT EDIT!

name: Publish

on:
  release:
    types:
      - published
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }} @ ${{ github.ref }}
  cancel-in-progress: true

jobs:
  pre:
    name: Pre
    permissions:
      actions: write
    runs-on: ubuntu-latest
    outputs:
      should-skip: ${{ steps.pre.outputs.should-skip }}
    steps:
      - id: pre
        name: Skip Duplicate Actions
        uses: liblaf/actions-ts/pre@dist
        with:
          cancel-others: true

  publish:
    name: Publish
    permissions:
      id-token: write
    needs:
      - pre
    if: needs.pre.outputs.should-skip != 'true' && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          registry-url: https://registry.npmjs.org
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
      - name: Install Dependencies
        run: |-
          bun install
          bun pm bin >> "$GITHUB_PATH"
      - name: Publish
        # TODO: switch to `bun publish` when <https://github.com/oven-sh/bun/issues/15601> is resolved
        run: npm publish
        env:
          # TODO: migrate to [Trusted publishing with OIDC](https://docs.npmjs.com/trusted-publishers)
          # ref: <https://github.com/npm/cli/issues/1637#issuecomment-1888709776>
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
